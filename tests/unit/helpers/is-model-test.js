import { isModel } from 'smack/helpers/is-model';
import { module, test } from 'qunit';

module('Unit | Helper | is model');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isModel([42]);
  assert.ok(result);
});
